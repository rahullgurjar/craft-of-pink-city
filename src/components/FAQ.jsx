import { useState } from 'react'
import { ChevronDown } from 'lucide-react'
import { faqs } from '../data/products'
export default function FAQ(){const [openIndex,setOpenIndex]=useState(0);return <section id="faq" className="bg-saffron/15"><div className="section-shell grid gap-12 lg:grid-cols-[.8fr_1.2fr] lg:gap-20"><div><p className="eyebrow">Helpful details</p><h2 className="section-title">Questions,<br/><i>answered.</i></h2><p className="body-copy">Everything you need to know before placing your order. For anything else, we are just a WhatsApp message away.</p></div><div className="border-t border-ink/15">{faqs.map(([question,answer],index)=><div className="border-b border-ink/15" key={question}><button className="faq-question" type="button" onClick={()=>setOpenIndex(openIndex===index?-1:index)} aria-expanded={openIndex===index} aria-controls={`faq-answer-${index}`}><span>{question}</span><ChevronDown className={`transition-transform duration-200 ${openIndex===index?'rotate-180':''}`} size={20}/></button><div id={`faq-answer-${index}`} className={`faq-answer ${openIndex===index?'faq-answer-open':''}`}><p>{answer}</p></div></div>)}</div></div></section>}
